# TODO - Fix TypeScript rootDir warning

- [x] Agregar `"rootDir": "./src"` en `frontend-angular/tsconfig.json` para eliminar el warning TS6 de migración.
- [x] Verificar con `npm run build` en `frontend-angular/` que ya no aparece el warning.
